{
  "name": "@jupyter-book/myst-plugin-iconify",
  "version": "0.2.0",
  "description": "MyST plugin to render inline icons via the Iconify API",
  "type": "module",
  "main": "dist/index.mjs",
  "scripts": {
    "build": "node build.mjs",
    "lint": "node --check src/*.mjs",
    "test": "npm run lint && vitest run",
    "test:watch": "vitest watch"
  },
  "keywords": [
    "myst",
    "plugin",
    "iconify",
    "icons",
    "markdown"
  ],
  "author": "Jupyter Book Team",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.19.0",
    "vitest": "^3.2.4"
  }
}
